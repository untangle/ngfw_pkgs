# NGFW-15626: Confirm conntrack on NF_DROP for marked UDP packets.
# Required on kernel 6.1+ where unconfirmed CT entries are destroyed
# when NFQUEUE issues NF_DROP at sub-INT_MAX priority, breaking the
# UDP proxy reply-matching path used by UVM.
options nfnetlink_queue nfqueue_confirm_drop_mark=255
