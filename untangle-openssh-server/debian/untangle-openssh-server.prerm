#! /bin/bash

SERVICE=untangle-ssh

case "$1" in
    remove|deconfigure)
      /etc/init.d/$SERVICE stop
      ;;
    failed-upgrade)
        ;;
    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 0
    ;;
esac

exit 0
