OSNAME=$(shell uname -s)
OPTFLAGS ?= -O2 -g
WARNFLAGS ?= -Wall -Wstrict-prototypes
CFLAGS += $(OPTFLAGS) -m32 $(WARNFLAGS) -I../include -D$(OSNAME) -D_GNU_SOURCE
PROGRAMS = bnx2id

all: ${PROGRAMS}

clean:
	@rm ${PROGRAMS}
