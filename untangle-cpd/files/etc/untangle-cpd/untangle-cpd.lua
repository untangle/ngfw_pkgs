

function cpd_replace_host( username, hw_addr, ipv4_addr )
   print( "Running replace host", username, hw_addr, ipv4_addr )
   print( "Host is greenlight", cpd_config.max_session_length )

   return false
end