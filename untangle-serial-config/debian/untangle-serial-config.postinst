#! /bin/bash

# Update grub for serial boot
update-grub

# Update systemd
systemctl daemon-reload
if [ `tty` != "/dev/ttyS0" ] ; then
    # Only restart serial connection if we're not in serial mode.  Otherwise we could interrupt a running script.
    systemctl restart serial-getty@ttyS0.service
fi

exit 0
