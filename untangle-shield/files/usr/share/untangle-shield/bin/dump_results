#!/bin/dash

delay=${1:-1}

while true ; do
    date
    curl -sf -F 'json_request=@-' -m 10 "http://localhost:3001" <<EOF
{ "function" : "dump_shield" }
EOF

    echo ""
    
    sleep ${delay}
done


