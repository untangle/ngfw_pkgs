#!/bin/bash

SERVICE=untangle-hardware-portwell-u50

case $1 in
  purge)
    rm -f /etc/init.d/$SERVICE
    systemctl disable untangle-hardware-portwell-u50 >/dev/null # lintian...
    ;;
esac

exit 0
