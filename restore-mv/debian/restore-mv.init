#!/bin/sh

case "$1" in
    start)
        if [ ! -z "`cat /proc/cmdline | grep edgeguard-restore`" ] ; then
          echo "Starting Edgeguard Restore."
          /usr/bin/edgeguard-restore
        fi
        ;;

    *)
        echo "Usage: /etc/init.d/restore-mv {start}"
        exit
        ;;
esac

exit 0