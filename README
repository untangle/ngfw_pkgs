How to create a private Debian repository:

1. Run deb-add.sh to add the .deb files to /var/www/metavize
	deb-add.sh /var/www/metavize foo.deb  [more .deb files]

2. Once all .deb files are added, run deb-scan.sh to recreate the Packages.gz file
	deb-scan.sh /var/www/metavize

Repeat the steps when you want to update any .deb file. 
If you add adding a new package, add an entry in override.testing.metavize. 
The fields for the entry are: package name, package priority, and package section.

To use the repository (atleast this example), add this line to your sources.list file:
deb http://bebe/metavize     testing metavize



Tools:
push.sh   
	sync to local repositories  
	./push.sh /var/www/dmorris /var/www/metavize

push-release.sh
	pushes /var/www/metavize to release.metavize.com
	removes non public transforms

push-release-alpha.sh
	pushes /var/www/metavize to release-alpha.metavize.com
	removes non public transforms

deb-add.sh 	
	addes deb files ($2+) to repository ($1)

deb-scan.sh 	
	Scans debian packages in repository ($1) and builds Packages list

clean-packages.sh 	
	clean-packages in the repository (used to clean out tons of old packages)
	"sudo ./clean-packages.sh /var/www/dmorris/pool/metavize/ 3 delete"
	deletes all but the newest three version of everything
