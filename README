http://bebe/mediawiki/index.php/In-house_package_management

Tools:
push.sh
    sync to local repositories
    ./push.sh /var/www/dmorris/ /var/www/untangle/

push-release.sh
    pushes /var/www/untangle to release.untangle.com
    removes non public transforms

push-release-alpha.sh
    pushes /var/www/untangle to release-alpha.untangle.com
    removes non public transforms

clean-packages.rb
    clean-packages in the repository (used to clean out tons of old packages)
    "sudo ./clean-packages.sh /var/www/dmorris/pool/untangle/ 3 delete"
    deletes all but the newest three version of everything
