<div id="<%= config_id %>">
  <p><%= "Select an interface to bridge with.".t %></p>
  <ul class="text-fields">
    <li>
      <label for="bridge"><%= "Bridge With:".t %></label>
      <%= select_tag( "#{config_id}.bridge_interface", options_for_select( [], nil )) %>
    </li>
  </ul>
</div>
