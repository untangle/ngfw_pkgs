<div>
  <h1>Static interface configuration for <%= @interface.name %></h1>

  <%= link_to_remote( "Add Network", :url => { :action => :create_new_network } ) %>
  <%= render(:partial => "static_ip_networks" , :object => @staticConfig.ip_networks ) %>
</div>
