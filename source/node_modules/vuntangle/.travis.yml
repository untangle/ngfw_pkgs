dist: jammy
language: node_js
node_js:
  - "20"
script:
- python3 "$TRAVIS_BUILD_DIR"/build/check_language_keys.py -v -s "$TRAVIS_BUILD_DIR"/src/ -t "$TRAVIS_BUILD_DIR"/src/locales/ || travis_terminate 1
- yarn install
- yarn build
