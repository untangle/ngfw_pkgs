<template>
  <span class="font-weight-bold" @click.prevent.stop="onClick" v-html="html"> </span>
</template>

<script>
  import Vue from 'vue'

  export default Vue.extend({
    computed: {
      html: ({ params }) => {
        if (params.click) return params.value.map(appl => `<a id="${appl.uid}">${appl.host}</a>`).join(', ')
        return params.value.map(appl => appl.host).join(', ')
      },
    },
    methods: {
      onClick(e) {
        if (!this.params.click) return
        if (e.target.id) this.params.click(e.target.id)
      },
    },
  })
</script>
