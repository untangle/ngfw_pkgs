/**
 * Upon component mount the incoming settings are validated against schema
 * This is to make sure that the settings data matches what component expects,
 * otherwise exceptions or invalid data change may occur
 */

export default {
  title: 'Example schema',
  type: 'object',
  properties: {
    prop1: { type: 'boolean' },
    prop2: {
      type: 'array',
      items: {
        type: 'object',
        properties: {
          subProp1: { type: 'boolean' },
          subProp2: { type: 'string' },
        },
        additionalProperties: false,
      },
    },
    prop3: {
      type: 'object',
      properties: {
        subProp1: { type: 'boolean' },
        subProp2: { type: 'string' },
      },
    },
  },
  required: ['prop1', 'prop2'],
  additionalProperties: false,
}
