/** defaults used in listing */
export const settingsDefaults = []

/** defaults used on add/edit static route */
export const staticRouteDefaults = {
  description: null,
  enabled: true,
  network: null,
  nextHop: null,
  interfaceId: null,
  metric: null,
}
