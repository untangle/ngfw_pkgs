<template>
  <div v-if="report" class="d-flex flex-column flex-grow-1">
    <u-grid
      id="reports-grid"
      :fetching="fetching"
      :column-defs="columnDefs"
      :row-data="data"
      @refresh="onRefresh()"
      @fetch-data="fetchData()"
    />
  </div>
</template>
<script>
  import reportMixin from './reportMixin'

  export default {
    mixins: [reportMixin],
  }
</script>
