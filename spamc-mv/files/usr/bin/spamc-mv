#!/bin/sh

if [ $# -ne 1 ]; then
    echo "usage: $0 msgfile";
    exit 1
fi

state=preamble

/bin/cat $1 | /usr/bin/spamc -R -t 15 | /usr/bin/awk '/[ -][0-9]+\.[0-9]+ [A-Z0-9_]+/ { print $1 " " $2 }'
