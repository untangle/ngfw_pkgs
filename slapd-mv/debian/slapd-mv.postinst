#!/bin/sh

# make it only accessible from localhost
sed -i 's/.*SLAPD_SERVICES=.*/SLAPD_SERVICES="ldap://127.0.0.1:389"' /etc/default/slapd


# start slapd (not always started by package install, somehow...)
/etc/init.d/slapd restart &> /dev/null &

exit 0 



