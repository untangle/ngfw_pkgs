#! /bin/bash

INITTAB=/etc/inittab

if ! grep -q monit $INITTAB ; then
  echo "mo:2345:respawn:/usr/share/untangle/bin/monit-wrapper.sh" >> $INITTAB
  [ -f /dev/initctl ] && telinit q
fi

touch /etc/untanglemonit-to-restart

exit 0