Just type ./configure && make all install, and normally it works
Of course, the frontend use the system command, so you must have
gocr and the viewer into your PATH.

Actually, if it doesn't work, you don't see it. gtk-ocr doesn't 
show an error box.

The name of the target file is the source filename + .txt
example
fax1.jpg gives fax1.jpg.txt
