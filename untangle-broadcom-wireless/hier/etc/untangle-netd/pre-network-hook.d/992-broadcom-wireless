#! /bin/bash

for nic in eth1 eth2 ; do
  hostapdConf="/etc/hostapd/hostapd.conf-$nic"
  if [ -f $hostapdConf ] ; then
    action=start
  else
    action=stop
  fi
  brcm-wifi $nic $action $hostapdConf
done

exit 0
