#!/bin/bash

SERVICE=untangle-hardware-portwell-m3000

case $1 in
  purge)
    rm -f /etc/init.d/$SERVICE
    systemctl disable untangle-hardware-portwell-m3000 >/dev/null # lintian...
    ;;
esac

exit 0
